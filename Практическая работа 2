1)Написать скрипт, который будет сравнивать разные типы данных и выводить итог их сравнения с пояснением. Данные для ввода объявлять в программе.
var a = 0;
 if (a == false); //true
 alert("true: переменные преобразовались числу 0");

var a = 0;
 if (a === false);
 alert("false: т.к. строгое равенство и типы различы");

var a = 0;
 if (null > a);
 alert("false: т.к. ноль не больше 0");

var a = 0;
 if (null >= a);
 alert("true: т.к. сравнение  приводит к числу, получается ноль);

var a = "Настя";
var b = "Эмиль";
 if ("Настя" > "Эмиль");
 alert("false: т.к. код у символа Э больше, чем у символа Н");

2)Написать скрипт, который будет принимать на ввод 2 однозначных числа и выводить наибольшее. Данные на вход брать из prompt.
 var a = prompt("Введите первое однозначное число");
 var b = prompt("Введите второе однозначное число");
 var c;
  if (a>b) { 
   alert(a);} else 
  if (b>a) {
   alert(b);} else
   alert("Числа равносильны");

3)Написать программу сравнения букв и выводить букву, которая в алфавите стоит раньше.
 var a = prompt("Введите первую буквенную переменную");
 var b = prompt("Введите вторую буквенную переменную");
 var c;
  if (a<b) { 
   alert(a);} else 
  if (b<a) {
   alert(b);} else 
   alert("Буквы равносильны");
